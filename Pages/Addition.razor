@page "/addition/fastmath"
@using Homework.Math.FastMath
<UiTimer>
    <AdditionFastMath></AdditionFastMath>
</UiTimer>


@code
{
    private int TopNumber { get; set; }
    private int BottomNumber { get; set; }
    private int? Answer { get; set; }
    private Random Random { get; } = new Random();
    private void NextQuestion()
    {
        TopNumber = Random.Next(0, 11);
        BottomNumber = Random.Next(0, 11);
    }
    private void HandleValidSubmit()
    {

    }
}